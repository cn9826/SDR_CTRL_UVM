git push -u origin master 
git push origin yourBranchName

git add <filename>
git commit -m "message about the commit"

git checkout <branch> //switch to a different branch
git pull origin master //when working on the master branch, get the changes from GitHub back to your local copy

git branch -d <branchName> // delete a branch locally
git push <remote_name> --delete <branch_name>

git fetch // download all the recent changes, but it will not put it in your current checked out code (working area)
git checkout origin/master -- path/to/file // git checkout <local repo name>/<branch name> -- path/to/file will checkout the particular file from the downloaded changes
